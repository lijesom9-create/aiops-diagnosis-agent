# 代码质量整改记录

> 目的：对 `education-agent` 项目做系统性的代码质量提升，每项改动遵循
> `ruff/扫描 → diff 人工审查 → 编译/回归验证 → 提交 → 记录`，确保不影响功能、可回滚。
> 起始时间：2026-09-05

## 一、扫描基线（改前现状）

执行 `ruff check backend demo-service` 与若干 grep 扫描：

| 维度         | 结果                                                                                            |
| ---------- | --------------------------------------------------------------------------------------------- |
| ruff 静态检查  | 109 项错误，其中 98 项可 `--fix`（42×I001 import排序 / 37×F541 f-string无占位 / 19×F401 unused-import）      |
| 裸异常        | `except:` / `except Exception` 共 353 处（待人工分类，见 T2）                                            |
| 绝对路径硬编码    | 4 处                                                                                           |
| TODO/FIXME | 2 处                                                                                           |
| 巨型文件       | database.py 2009 / agent.py 1883 / unified\_store.py 1788 / tools.py 1127 / alerts.py 1105（行） |
| 敏感信息       | `.env` 已被 git ignore ✓（未追踪）                                                                   |
| 依赖管理       | backend 用 `pyproject.toml` + `requirements*.txt`                                              |

## 二、T1：ruff 机械项自动修复 ✅

**范围**：仅对 `backend` 与 `demo-service` 的已跟踪 `.py`，`--select I001,F401,F541 --fix`。

**实际改动 4 个文件（全部等价机械调整）**：

- `demo-service/main.py`：重排 `import db / import faults` 位置（I001），**未删除副作用 import**，初始化链路不变。

- `demo-service/verify_matrix.py`：删多余空行；`f"\nR5 故障注入验证矩阵"`→普通字符串（F541）。

- `backend/scripts/run_ablation_1_3_4.py`：删未使用的 `hybrid_rrf_pc_search` import（F401）。

- `backend/scripts/seed_demo_kb.py`：import 排序（I001）。

**验证**：`python -m compileall -q backend demo-service` 通过（语法无误）；diff 逐行人工审查确认无副作用删除。

**说明**：另有 `backend/scripts/` 下 37 个开发/一次性脚本（未跟踪、不进版本库）亦被同批修复，结果有效但无需提交；不影响运行核心。

**遗留问题（记入 T2/T4）**：`backend/evaluation/perf/e2e_qps_test.py`、`qps_test.py` 的 docstring 存在非法转义序列 `\S`，触发 `SyntaxWarning`。

## 二·补、T2：裸异常人工甄别（收敛处理）✅

**结论**：AST 画像——`except` 共 450 处，其中"纯静默(pass/空)"52 处。逐一阅读后端核心后判定：**绝大多数是"合理降级的次要路径"，并非掩盖故障的危险吞错**。典型形态：

| 形态     | 实例                                                                          | 判定          |
| ------ | --------------------------------------------------------------------------- | ----------- |
| 指标埋点失败 | alerts.py `get_metrics().increment(...)` ×5、incident\_ingest、token\_counter | 指标采集不该打断主流程 |
| 临时文件清理 | parser/docling/multimodal/ocr 的 `os.unlink`                                 | 清理失败无碍      |
| 超时轮询   | alerts.py `asyncio.wait_for`                                                | 等唤醒信号，故意    |
| 解析兜底   | monitoring 时间格式、parser 编码                                                   | 拉取兜底        |

**学习点**：静态扫描只能给"疑似"线索，"是否真问题"必须读上下文。机械补 error 日志/重抛会制造噪音，属错误方向。

**落地动作**（低侵入规范化）：为 9 处缺理由的"合理忽略"补 `# 为何忽略` 注释

- `backend/app/api/alerts.py` 7 处（指标埋点×6 + 轮询超时×1）

- `demo-service/faults.py`、`db.py` 各 1 处

- 验证：compileall 通过；diff 逐行审查确认只加注释、无逻辑变更。

**状态**：T1、T2 完成；T3 部分完成（agent.py 证据簇）；T4 待办。

## 二·再补、T3-A：拆分 agent.py——证据解析簇抽离为 `evidence.py` ✅

**对象**：`backend/app/langgraph_agent/agent.py`（1883 行）

**目标**：把证据解析这块与 `self` 无依赖的纯函数抽成独立模块，减小 agent.py、便于单测与复用。

**分析**：agent.py 中 13 个 `_parse_*` / `_build_*` / `_collect_*` / `_merge_*` / `_extract_*` & `_compute_evidence_sufficiency` 方法只吃 `messages/data` 入参、输出结构化 dict/字符串，**不读** **`self`** **状态** → 可整体抽出为纯函数。

**落地**：

- 新建 `backend/app/langgraph_agent/evidence.py`（631 行）：迁移全部 13 个方法为模块级纯函数，补全 `from __future__ import annotations` 与 `AIMessage/ToolMessage` 导入。

- `agent.py`：删 699 行方法定义，改为 `from .evidence import (...)`；`self.method()` 调用全部换成直接函数调用；净减少约 699 行。

- 保留注释（如 ToolMessage.artifact 丢失的背景、为何结构化提取监控证据）以免丢失设计语境。

**验证**：

- `python -m compileall` agent.py + evidence.py 通过（exit 0）

- grep 确认 agent.py 无残留方法定义、无 `self._xx` 调用残留

- 纯函数无 `self` 依赖，逻辑与迁移前逐字等价（仅删 `self.` 前缀）

**状态**：T1、T2、T3-A 完成；剩余 T3（database.py / unified\_store.py 拆分）与 T4 待办。

**补记（回归修复，2026-09-05）**：T3-A 首提后跑 `tests/test_agent_graph_flow.py` 等回归发现 **9 失败**，两层根因：

1. 抽取证据方法时把它们的 `@staticmethod` 装饰器残余留在了 agent.py —— 11 处悬空装饰器误加到实例方法上（`_count_message_tokens`、`_get_sufficiency_for_run`、`_build_system_prompt`），导致 `self` 方法被当成静态方法，"缺 state" 报错。→ 已全部移除，仅保留 3 处本就合法的 `@staticmethod`。
2. 被抽方法原本是 `LangGraphAgent` 的 `@staticmethod`，被 **6 个非生产调用方**（测试/脚本/评测）以 `LangGraphAgent._xxx()` 直接引用，抽离后类属性消失 → `AttributeError`。→ 已把这些调用方改为 `from app.langgraph_agent.evidence import ...`（test\_evidence\_parse、test\_agent\_graph\_flow、test\_business\_features、test\_reasoning\_layer、scripts/verify\_agent\_diagnosis、evaluation/verify/verify\_citations）。
3. **教训**：抽静态方法出类时，需全局搜索 `ClassName._method()` 调用方（含 tests/eval/scripts），不能只看编译/类内残留——compileall 对"类属性访问运行时才失败"不报错。

**验证**：compileall 通过；`test_agent_graph_flow + test_business_features + test_reasoning_layer` **51 passed**。

## 二·补、T3-B①：拆分 database.py——users+content 抽为 Mixin ✅

**对象**：`backend/app/core/database.py`（2009 行，单类 `Database`，约 110 方法，双后端：MongoDB / 内存）

**方案**：Mixin 组合（保持 `get_db()` 对外不变）。迁移第一批 `users+content`：

- 新建 `app/core/db_utils.py`：搬出 3 个无依赖 helper（`escape_regex`/`clean_mongo_doc`/`clean_mongo_docs`），供 mixin 与 database.py 复用，避免循环导入；database.py 顶部 re-export（保持 `from app.core.database import clean_mongo_doc` 兼容）。

- 新建 `app/core/db_mixins/` 包：`users.py`(class UsersMixin：用户/认证/token-version/会话/进度)、`content.py`(class ContentMixin：主题/文档/计划/组织)。

- `database.py`：删除这两组方法 + 3 个 helper，`class Database(UsersMixin, ContentMixin):`，净减约 598 行。

- 方法体 verbatim 搬移（仅换类名），跨组 `self.xxx()` 调用天然仍可用（Mixin 组合）。

**边界与工具**：方法行范围用 `ast` 精确取（含装饰器/docstring），不用正则枚举（正则对多行/嵌套边界不可靠——T3-A 的教训）。

**回归踩坑（重要）**：把一段"依赖模块顶层导入 `copy/re/datetime`"的函数搬走后，若目标模块不补这些 import → 运行期 `NameError`（compileall 不报，仅运行/测试暴露）。首批缺失 `copy`/`re`/`datetime`/`uuid`/`Dict`/`Tuple`，已逐一补全。**教训**：拆分后必须跑全量测试（不能只靠 compileall/import 检查）才能捕获这类未定义名。

**验证**：compileall 通过；全量 `tests/` 第一跑 488 项（fail=5，其中 3 个 db\_utils 漏引所致、修后通过；2 个 `langgraph.checkpoint.mongodb` 为既有依赖缺失，与拆分无关）；修复后应全绿（除既有 2 项）。

## 二·补、T3-B②：拆分 database.py 剩余域——incidents/diagnosis/knowledge 抽为 Mixin ✅

**对象**：T3-B① 后 `backend/app/core/database.py`（1666 行，62 个域方法）。

**方案**：用 `split_db_batch2.py`（AST 精确取方法边界）把剩余 60 个方法 verbatim 迁移为 3 个新 Mixin：

- `db_mixins/incidents.py`（class IncidentsMixin：事故/事故指纹/下线诊断记录/事故模式聚合）14 方法

- `db_mixins/diagnosis.py`（class DiagnosisMixin：诊断任务队列/自愈问题/反馈/审计日志/待复习标记）10 方法

- `db_mixins/knowledge.py`（class KnowledgeMixin：知识/课程/知识点/学习会话/长期记忆/工作流状态等）36 方法

- `class Database(UsersMixin, ContentMixin, IncidentsMixin, DiagnosisMixin, KnowledgeMixin)`，database.py 由 1666 → 299 行

- `db_mixins/__init__.py` 追加导出新 Mixin

**导入自检（本次重点，避免重蹈①的 NameError）**：脚本按"文本内实际出现"补顶层 import（datetime/timedelta/uuid/typing/logger/settings/db\_utils helpers/asyncio）。行前用 AST 扫描每个方法组体，逐个核对 `re.match` 出的符号名是否被该休息覆盖：

- `copy` 全程为 `.copy()` 方法调用（dict/list/str），**非模块** → 无需 `import copy`

- `DuplicateKeyError` / `defaultdict` 均为**方法体内局部 import**（`from pymongo.errors import ...` / `from collections import ...`），verbatim 复制后随之带走 → 无需顶层补

- 唯一漏项：脚本改 `__init__.py` 但**没同步 database.py 顶部 import** → 首跑 `NameError: IncidentsMixin`。手工把第 11 行补成 `from app.core.db_mixins import ContentMixin, DiagnosisMixin, IncidentsMixin, KnowledgeMixin, UsersMixin`。

- 删 60 方法后 database.py 顶部 `timedelta`/`Optional` 变未用 → ruff --fix 移除（I001/F401 全绿）。

**验证**：compileall 通过；`test_e2e_fixes.py + test_admin_api.py` 25 passed；全量 `tests/` **486 passed, 3 skipped**，仅剩 2 个既有的 `langgraph.checkpoint.mongodb` 依赖缺失（与拆分无关，同 T3-B① 基线一致）。

## 二·补、T3-C：unified\_store.py 保守拆分——抽纯函数为 store\_utils ✅

**对象**：`backend/app/knowledge/unified_store.py`（2037 行，核心 `UnifiedKnowledgeStore` 类 1616 行 / 40 方法）。

**前置评估（为什么不照搬 database.py 的 Mixin 全拆）**：与 Database 的"天然按业务域分块"不同，`UnifiedKnowledgeStore` 的检索主流程高度内聚（search→\_vector\_search→\_rrf\_fuse→\_select\_rerank\_candidates→\_rewrite\_query 一路互调），且 `__init__` 承载大量实例状态（embedding/reranker/CLIP/多级缓存/双 store）。全拆会切成"一堆互调方法的中层类"，反而损可读性，且触检索语义硬约束、回归风险高。**故采用用户选定的保守拆**。

**方案（尽力保持行为逐字节等价）**：

- 新建 `app/knowledge/store_utils.py`：迁出 6 个**无状态纯函数**（不访问 self）：`merge_filters` / `match_metadata_filter` / `build_visibility_filter` / `build_child_filter` / `rrf_fuse` / `tokenize`。verbatim 搬移，递归改自引用模块函数。

- `UnifiedKnowledgeStore` 保留同名**薄委托方法**（`@staticmethod` 或按原样 instance 方法，签名不变），把逻辑转调 store\_utils。这样**所有既有调用点零改动**：内部 `self._merge_filters(...)`、类级 `UnifiedKnowledgeStore._match_metadata_filter(...)`（本文件递归 + 测试文件 4 处）、外部 `self._store()._build_visibility_filter(...)` 全部保持可用 → 对外契约稳定、无需改任何业务调用方。

**导入自检**：

- `_tokenize` 用到 `re`——搬走后 unified\_store 在 `QueryRewriter`（`re.search/re.escape`）仍用，故 `import re` 保留不删。

- `_tokenize` 内 `jieba` 为局部 import，随体搬走，store\_utils 仅在需要时触发。

- store\_utils 仅依赖 `typing`+`re`（及惰性 jieba），无循环导入风险。

**委托一致性验证**：委托方法 vs 模块函数对相同输入输出逐项相等（merge\_filters/match\_metadata\_filter/tokenize/rrf\_fuse 抽样比对 OK）；delegator parity OK。

**验证**：compileall 通过；ruff（unified\_store.py + store\_utils.py）全绿；检索相关测试 `test_retrieval_eval`/`test_reranker_maxp`/`test_generation_eval`/`test_execution_reliability` **22 passed**；全量 `tests/` **486 passed, 3 skipped**（仅余既有 checkpoint 依赖缺失 2，同基线一致）。

## 三、T4：框架版本更新评估 ✅

> 本项为**评估**（产出版本差距矩阵 + 升级破坏面 + 推荐方案），不直接改依赖。结论：**当前版本均可安全小升，无需大版本冒险**；唯一需联动处理的是 LangGraph 与其 checkpoint 包的版本耦合。

### 3.1 版本差距矩阵（2026-09-05 实测本机 ／ 需求文件 ／ PyPI 最新）

| 包                            | 本机实际       | requirements.txt 声明 | PyPI 最新        | 差距       | 破坏面分档      |
| ---------------------------- | ---------- | ------------------- | -------------- | -------- | ---------- |
| fastapi                      | 0.115.12   | 0.115.12            | 0.141.1        | 滞后约 26   | 中（有 break） |
| uvicorn                      | 0.34.0     | 0.34.0              | —              | 小幅       | 低          |
| langgraph                    | 1.1.6      | 1.1.6               | 1.2.11         | 滞后 15    | 低          |
| langgraph-checkpoint-sqlite  | 3.0.3      | 3.0.3               | 3.1.1          | 小幅       | 低          |
| langgraph-checkpoint-mongodb | **未装**     | 0.4.0               | 随 langgraph 联动 | —        | 测试盲区       |
| langchain-core               | 1.5.3      | 1.5.3               | 1.6.x          | 滞后约 8    | 低          |
| langchain-openai             | 1.1.12     | 1.1.12              | 小幅             | —        | 低          |
| langchain-mcp-adapters       | 0.3.1      | 0.3.1               | **已被取代**       | —        | 前瞻项        |
| qdrant-client                | 1.17.1     | 1.17.1              | 1.19.0         | 滞后 2     | 低          |
| chromadb                     | 1.5.8      | 1.5.8               | 1.5.9          | 小幅       | 低          |
| pydantic                     | **2.12.5** | **2.13.5**          | 2.13.x         | 声明与实装不一致 | 需对齐        |
| pymongo                      | **4.17.0** | **4.16.0**          | —              | 声明与实装不一致 | 需对齐        |

> 注意：`pydantic`、`pymongo` 的**本机实装版本与 requirements.txt 声明不一致**——声明是给 Docker/生产用的，本机 venv 是更早一次 `pip install` 的快照。评估以声明（=上生产者）为准。

### 3.2 逐项破坏面分析（已读项目实际调用点）

**FastAPI 0.115.12 → 0.141.1（滞后最多，也最关键）**

- 已确认本项目**只用标准路由/依赖注入/Response，不迭代** **`router.routes`**，故 0.137 把 `router.routes` 改为树结构这一破坏点**不影响**本项目。

- 0.132 起**默认严格校验 JSON 的** **`Content-Type`**（缺 `application/json` 报错）→ 需核对前端/测试桩是否都带该头；不符加 `strict_content_type=False`。

- 0.129 起要求 Python≥3.10（本项目 py312，达标）；0.128 起弃用 Pydantic v1 兼容层（本项目全 v2，达标）。

- 0.130+ 默认 JSON 序列化走 Pydantic Rust 快路径（对带 `response_model` 的路由是 2× 提速，零改动）；0.134+ 原生支持 `yield` JSONL/SSE 流式（后续做 Agent token 流式输出可少写自定义流式代码）。

- 项目大量用了 SSE 端点（`/api/langgraph/.../stream`）：0.136 起对 SSE/JSONL 的 `status_code`、`format_sse_event` 做了绑定修复，升级属**修 bug** 方向。

**LangGraph 1.1.6 → 1.2.11（低风险）**

- 1.1 新增 `version="v2"` 流式/`invoke` 类型化输出，但**默认仍是 v1**；项目用 `graph.ainvoke(...)` / `MemorySaver` / `AsyncSqliteSaver` / `MongoDBSaver`，均落 v1 契约，**无需改动**。

- 1.2.x 主要是 subgraph replay / delta channel / stream\_events 类型注解的修复与增强，项目未用这些新通道。

- **真正的耦合**：langgraph 1.2.x 会把 checkpoint 包一同调版（sqlite 3.0.3→3.1.x，mongodb 需对应升高）。`langgraph-checkpoint-mongodb` **本机未安装**（注释注明此二测试缺失属既有基线）——生产才用到，故 Docker 中验证 mongodb checkpoint 兼容是升级前置动作。

**qdrant-client 1.17.1 → 1.19.0（低风险，已抓关键证据）**

- 1.19 移除 `client.add` / `client.query`（FastEmbed 便捷法）；**项目** **`learn`** **已全部用** **`query_points()/scroll()/upsert()/create_collection()`** **新 API**（[qdrant\_store.py](file:///d:/Dev/Projects/my-code-space/projects/claude-learning/education-agent/backend/app/retrieval/qdrant_store.py) L399/455/838 等），**不受移除影响**。

- 1.19 弃用 `search()/search_batch/on_disk_payload`——项目未用 `client.search()`、`create_collection` 未传 `on_disk_payload`（`payload=` 均为 query/scroll 参数），**零需要改**。

- 1.19 弃用 py3.9（py312 达标）；"adjust numpy versioning" → 本机 numpy 2.4.3 需在升级时验兼容。

**langchain-mcp-adapters 0.3.1（前瞻项，不阻塞）**

- langchain 1.4.0 起 MCP 能力内置 `langchain.mcp`（`MCPAdapter`），`MultiServerMCPClient` 由迁移指南取代。项目 [agent.py L196](file:///d:/Dev/Projects/my-code-space/projects/claude-learning/education-agent/backend/app/langgraph_agent/agent.py#L194-L196) 仍用 `MultiServerMCPClient(mcp_config)` 加载 ops\_monitoring\_server。**0.3.1 当前仍可用、无强制升级**；等 MCP 适配器生命周期再评估是否迁到 `langchain.mcp`（会动 MCP 集成链路，风险中等，宜单独排期）。

**chromadb / pydantic / pymongo**

- chromadb 1.5.8→1.5.9 补丁级，低风险，可顺手。

- `pydantic` 按声明对齐到 2.13.x（langchain-mongodb 要求 >=2.13），本机 2.12.5 需补装。

- `pymongo` 声明 4.16.0 是为了兼容 langchain-mongodb 的 `pymongo<4.17` 上界，但本机已是 4.17.0——**两条约束冲突**，需先确认目标 langgraph-checkpoint-mongodb 版本要求后统一，避免生产 `pip install` 解析冲突。

### 3.3 推荐方案

| 优先级          | 动作                                                          | 风险           | 验证                                         |
| ------------ | ----------------------------------------------------------- | ------------ | ------------------------------------------ |
| P1（顺手、零代码改动） | chromadb 1.5.8→1.5.9；pydantic 对齐 2.13.x                     | 低            | 全量 tests                                   |
| P2（低风险，收益明确） | qdrant-client 1.17.1→1.19.0                                 | 低（代码已用新 API） | 检索相关测试 + 本机 Qdrant server 冒烟               |
| P3（中风险，需联动）  | langgraph 1.1.6→1.2.11 **连同** checkpoint-sqlite/mongodb 一起升 | 中            | 先 Docker 验 mongodb checkpoint；Win 验 sqlite |
| P4（中风险，独立排期） | FastAPI 0.115.12→0.141.1                                    | 中            | 核对严格 Content-Type 后全量回归 + SSE 冒烟           |
| P5（前瞻，单独评估）  | langchain-mcp-adapters → langchain.mcp(`MCPAdapter`)        | 中            | MCP 监控链路端到端                                |

> 通用护栏：每次升一个包 + 跑 `python -m compileall` 与全量 `tests/`（现基线 486 passed / 3 skipped）；先 git 提交现状再改。

### 3.4 升级风险摘要（一句话）

**低风险项可随时做；FastAPI 是唯一有破坏点的（0.132 严格 Content-Type），LangGraph 升级必须与 checkpoint 包捆绑并先在 Docker 验 mongodb 分支。**

### 3.5 P1+P2 已实际执行 ✅（2026-09-05）

**升级动作**（按护栏"先提交现状→逐个升级→全量回归→记录"）：

1. `chromadb 1.5.8 → 1.5.9`（patch 级）
2. `pydantic 2.12.5 → 2.13.5`（本机实装对齐 requirements.txt 声明；pydantic-core 2.45.2→2.46.5）
3. `qdrant-client 1.17.1 → 1.19.0`

**前置环境问题（非任务）**：本机 `REQUESTS_CA_BUNDLE` 环境变量指向失效旧路径 `D:\pythonProject\.venv\pythonProject2\...\certifi\cacert.pem`，导致 `pip install` TLS 握手失败。临时在本条命令覆盖为当前 Python 的 `certifi.whereat()` 后正常；属既有环境污染，已记录排查法。

**验证**：

- 版本核验：chromadb 1.5.9 / pydantic 2.13.5 / qdrant-client 1.19.0 ✓

- `python -m compileall -q app` 通过 ✓

- 冒烟 import `QdrantVectorStore` / `UnifiedKnowledgeStore` 通过 ✓（qdrant 1.19 移除 `client.add/query`、弃用 `search`——项目已用新 API 不受影响）

- 定向检索测试 `-k "retrieval or reranker or chroma or qdrant"`：**10 passed / 1 skipped** ✓

- 全量 `tests/`：**2 failed / 486 passed / 3 skipped**（与升级前基线**完全一致**，2 个失败仍为既有 `langgraph.checkpoint.mongodb` 缺失项，非本批引入）✓

**回滚**：若需回退，将三包降至原版本即可（说明：`~hromadb` 旧残留目录不影响，忽略）。

**后续**：P3(LangGraph 1.2.11 捆绑 checkpoint)、P4(FastAPI 0.141.1)、P5(MCP 迁移) 均未执行，留待单独排期。

***

状态：T1、T2、T3-A、T3-B①、T3-B②、T3-C(保守拆)、T4(评估+P1/P2 执行) 完成；P3/P4/P5 待排。（2026-09-05）
