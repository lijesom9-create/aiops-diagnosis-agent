# 看板自动供应：从 /var/lib/grafana/dashboards 读取（compose 只读挂载）
apiVersion: 1

providers:
  - name: ops-agent
    folder: Ops Agent
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
