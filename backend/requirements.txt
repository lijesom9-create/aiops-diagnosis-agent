# Web 框架
fastapi==0.115.12
uvicorn[standard]==0.34.0
python-multipart==0.0.20  # FastAPI 表单/文件上传（UploadFile/Form）所需

# 数据库（MongoDB）
pymongo==4.17.0  # motor 底层驱动；langgraph-checkpoint-mongodb 0.5.0 约束 pymongo>=4.12,<4.18（4.17 满足）
motor==3.7.1  # 异步 MongoDB 驱动

# 认证与安全
PyJWT==2.11.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # pin 版本以兼容 passlib 1.7.4
email-validator==2.3.0  # pydantic.EmailStr 所需

# AI 模型 / LLM
anthropic==0.97.0
openai==2.30.0
httpx==0.28.1  # 异步 HTTP 客户端（AI/VLM API 调用）
tiktoken==0.12.0  # token 计数

# LangChain / LangGraph（注：版本与已验证可运行的本地环境对齐，LangChain 1.x 系列）
langchain==1.4.0  # langchain-checkpoint-mongodb 0.5.0 经 langchain-mongodb 引入；含 beta 的 langchain.mcp 命名空间
langchain-openai==1.1.12
langchain-core==1.6.2
langchain-text-splitters==1.1.1  # 文档分块
langchain-mongodb==0.12.0  # langgraph-checkpoint-mongodb 0.5.0 的依赖（pymongo>=4.12,<4.18）
langgraph==1.2.11
langgraph-checkpoint==4.2.0  # langgraph 1.2.11 要求 langgraph-checkpoint>=4.1.0
langgraph-checkpoint-sqlite==3.1.1  # LangGraph SQLite 持久化检查点
langgraph-checkpoint-mongodb==0.5.0  # LangGraph Mongo 检查点（多副本共享会话，CHECKPOINT_BACKEND=mongodb）
prometheus-client==0.25.0  # /metrics 指标暴露（应用自观测，Prometheus 采集）
aiosqlite==0.22.1  # 异步 SQLite（AsyncSqliteSaver 所需）

# MCP (Model Context Protocol) 监控工具集成
# 运维诊断 Agent 通过 MultiServerMCPClient 加载 mcp_servers/ops_monitoring_server 的
# query_metrics / query_logs 工具；缺这两个包会导致 Docker 中 MCP 加载失败降级
langchain-mcp-adapters==0.3.1  # MultiServerMCPClient 加载 MCP 工具（需 langchain-core>=1.3）
mcp==1.27.0  # FastMCP（ops_monitoring_server 使用）

# 向量数据库与语义嵌入
chromadb==1.5.9
sentence-transformers==5.4.1  # Embedding 与 CrossEncoder 重排器
FlagEmbedding==1.4.0  # BGE-M3（dense + sparse 同源，sparse 替代自研 BM25）
qdrant-client==1.19.0  # Qdrant 向量库

# 机器学习 / 重排器 / CLIP
torch==2.13.0+cpu  # 模型推理（CPU 版本，配合 --extra-index-url 使用）
transformers==4.57.6  # CLIP / AutoTokenizer
optimum[onnxruntime]==2.1.0  # ONNX 推理加速（CrossEncoder 重排器）
numpy==2.4.3

# 文档解析
pypdf==6.10.2  # PDF 解析
PyPDF2==3.0.1  # PDF 解析（app/document/parser.py 直接 import）
python-docx==1.2.0  # Word 文档解析
PyMuPDF==1.27.2.3  # fitz，PDF 解析与渲染
docling==2.115.0  # 高级文档解析
pdfplumber==0.11.10  # PDF 表格解析
pillow==12.2.0  # PIL，图片处理
pdf2image==1.17.0  # PDF 转图片（OCR，app/document/ocr.py；需系统 poppler-utils）
chardet==7.4.3  # 文本编码检测

# 日志
loguru==0.7.3  # 结构化日志（main.py 及各 API 模块直接 import）

# 中文 NLP 分词
jieba==0.42.1  # 中文分词（BM25/检索/记忆模块）

# 缓存
redis==5.3.1  # 可选缓存与限流后端

# 异步任务队列（文档导入异步化）
celery==5.5.3  # 分布式任务队列，broker 复用 Redis

# 工具库
pydantic==2.13.5  # >=2.13 为 langchain-mongodb（checkpoint 依赖）的最低要求
pydantic-settings==2.14.2
